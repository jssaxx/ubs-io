# 安全策略

UBS IO 涉及本地缓存、KV Cache、块设备路径、动态库加载和生态组件集成。安全问题请优先私下报告，避免在公开 Issue 中披露可利用细节。

## 报告方式

请通过项目所在托管平台的安全报告能力、私有 Issue、组织安全联系人或维护者指定渠道提交。报告中建议包含：

- 受影响模块和版本或 commit。
- 漏洞类型、影响范围和潜在攻击条件。
- 复现步骤、PoC、日志或配置片段。
- 已知缓解方式。
- 是否已经公开披露或提交给其他上游社区。

如果当前平台没有私有安全报告能力，请先创建不含利用细节的普通 Issue，请求维护者提供私下沟通渠道。

## 响应周期

- 2 个工作日内确认收到报告。
- 5 个工作日内完成初步分级并确认处理负责人。
- 高危问题优先准备修复和缓解说明。
- 修复发布后同步安全公告、受影响版本和升级建议。

响应周期可能受硬件环境、上游依赖和复现条件影响；维护者会在私下沟通中同步进展。

## 安全边界

以下内容通常属于 UBS IO 安全处理范围：

- UBS IO 代码、脚本、默认配置或文档导致的权限、数据泄露、任意文件访问、命令执行、越界访问、拒绝服务等问题。
- `ubsio-kv`、`ubsio-boostio` 和 `ubsio-common` 中的接口误用风险。
- Mooncake patch、memcache 集成、BoostIO 动态库加载和块设备路径处理中的安全缺陷。

以下内容通常需要与上游或部署方共同处理：

- 操作系统、驱动、固件、NPU Runtime、Zookeeper、memcache、Mooncake、vLLM-Ascend 等外部依赖自身漏洞。
- 业务侧错误权限配置、明文密钥管理或生产环境网络隔离缺失。
- 未按文档要求部署导致的环境暴露。

## 披露流程

1. 报告人私下提交漏洞信息。
2. 维护者完成复现、影响分析和分级。
3. 维护者准备修复、测试和临时缓解方案。
4. 修复合入并发布后，公开安全公告。
5. 公告中感谢报告人，除非报告人要求匿名。

请不要在修复发布前公开 PoC、利用细节或受影响部署信息。
