file(GLOB_RECURSE BIO_SECURITY_SRCS *.cpp *.h)
add_library(bio_security SHARED ${BIO_SECURITY_SRCS})
if (UBSIO_ENABLE_ORIGIN_RUNPATH)
        set_target_properties(bio_security PROPERTIES
                BUILD_RPATH "$ORIGIN"
                INSTALL_RPATH "$ORIGIN"
        )
endif()
install(TARGETS bio_security DESTINATION ${TARGET_INSTALL_LIB_DIR}/ PERMISSIONS OWNER_WRITE OWNER_READ OWNER_EXECUTE GROUP_READ GROUP_EXECUTE WORLD_READ WORLD_EXECUTE)
